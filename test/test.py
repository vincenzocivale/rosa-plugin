# import roslibpy

# host = "172.20.0.1"
# port = 9090

# print(f"Trying to connect to ROS server at {host}:{port}")
    

# ros_client = roslibpy.Ros(host=host, port=port)

# ros_client.on_ready(lambda: print('Is ROS connected?', ros_client.is_connected))
# print(ros_client.is_connected)